- debug: msg="{{ lookup('env','HOME') }}"

- name: copy sudoers_ansible
  copy:
    src: files/zsh-autosuggestions.zip
    dest: "{{ lookup('env','HOME') }}"
    owner: root
    group: root
    mode: 0440   
