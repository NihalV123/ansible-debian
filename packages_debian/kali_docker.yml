--- 
- name: Make sure a docker service is running
  service:
    state: started
    name: docker
- name: install debian dockers
  docker_image:
      source: pull
      name: "{{ item }}"
  loop:
  - alpine
  - linuxserver/guacd
